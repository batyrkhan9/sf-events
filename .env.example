# Copy to .env and fill in. .env is gitignored.

# Required: used for event extraction and filtering.
ANTHROPIC_API_KEY=

# Optional: without these, the digest prints to the console instead.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
